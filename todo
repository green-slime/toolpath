1、展示初始化曲面与目标曲面的误差，（多方面展示）。
2、优化后在 RMS 距离和 Hausdorff 距离这两个指标中是否有明显的改善。
(需要回答下方的问题)：
这是一定的，我们做两个不同的问题：
（1）优化 RMS 距离；观察渲染结果是否有改善。（也不一定，这里需要直接使用曲面几何loss）
（2）优化渲染结果，这样与曲面几何loss应该没有关系了。
3、如果以最小化两个距离为目标，观察渲染结果的差异。
4、大量采样，做一次实验。
5、g 代码生成。（路径本身的限制，和具体机器相关）
6、bdc 修改。

问题：
两组不同的曲面可能对应一个渲染结果，如何避免优化成另一个曲面？

bug:
怎么差了一个R的距离啊！
好坑，经实验：
1、ballcutter 第一个参数为直径；
2、算出来的 cl_paths 根本没有半径补正，算的是刀头最底下的距离。需要抬高一个半径的高度。
3、cl_paths 还有默认最低 z 值，导致之前的路径都在 z=0 的平面上。所以，bdc.appendPoint(ocl.CLPoint(x, y, -0.1)) 的 z 值应设为网格的最低点。

评判曲面精度：
scallop height 不能超过指定值；不能发生 gouge 现象（gouge-free）。
另外总路径长度越短越好。



#################### 20250805 ######################
新的想法：
1、对落点 loss 做 SGD，然后再对渲染 loss 做全梯度。
2、对渲染 loss 做 SGD，具体方法是每次采随机的不同采样点。